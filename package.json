{
  "name": "mdcov",
  "version": "1.0.1",
  "module": "dist/index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build index.ts --target node --outdir dist --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@actions/cache": "^4.0.3",
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  }
}
